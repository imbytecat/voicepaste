# VoicePaste 隐私说明

生效日期：2026-08-08

本说明适用于 VoicePaste 桌面应用 1.0.0。

## 数据处理概览

VoicePaste 不运营自建服务器，也不包含自建遥测、行为分析、广告 SDK 或崩溃上报服务。应用只在完成用户请求所必需时访问本机能力和豆包语音识别服务。

## 麦克风与豆包语音识别

- 应用仅在用户主动触发听写后采集麦克风音频；完成、取消或出错后停止采集。
- 听写期间，音频以流式方式直接发送到豆包语音识别服务，用于生成文字。VoicePaste 不在本机保存录音，也不把音频发送到 VoicePaste 自建服务器。
- 用户配置的常用词保存在本机，并在保存设置时创建或更新用户火山引擎账户下的专用词表。开始听写时，VoicePaste 只向豆包发送该词表的内部标识，不再逐次发送完整词表。
- “测试连接”会向豆包发送 API Key 和必要的服务请求元数据，并读取云端词表状态，但不会采集或发送麦克风音频。
- 豆包对请求数据的处理受火山引擎账户配置、服务协议和隐私政策约束；VoicePaste 无法控制第三方服务端的留存策略。用户应在使用前确认其账户和所在地区适用的条款。

## API Key

- 豆包 API Key 用于建立语音识别连接，会作为认证信息发送给豆包。
- 新保存的 API Key 使用操作系统凭据库：macOS 钥匙串、Windows 凭据管理器或 Linux Secret Service/兼容凭据库。
- 若系统凭据库不可用，保存会失败并要求用户处理系统凭据库问题；应用不会把新的 API Key 静默降级为明文设置。
- 受限回退仅用于旧版本迁移：若旧设置中存在历史回退值，应用会尝试一次性迁移到系统凭据库；迁移失败时会清除该本地值并要求重新填写，不会继续以明文形式保留。

## 本地数据

以下数据保存在本机应用数据目录：

- 全局快捷键与触发模式；
- 麦克风选择；
- 常用词的本地副本及其云端词表绑定标识；
- 首次设置完成状态、开机启动选择、浮层位置等普通设置。

关闭开机启动或卸载应用不一定会自动删除操作系统凭据库、应用数据目录或火山引擎账户中的专用词表。需要彻底清除时，请先在设置中清空常用词并保存，再清空 API Key 并保存，最后按操作系统方式删除 VoicePaste 的应用数据。

## 日志

VoicePaste 会在本机写入有限的运行日志，用于诊断启动、权限、快捷键、输入和更新问题。日志采用大小和数量限制进行轮换。

应用日志不记录豆包 API Key、原始音频数据或识别正文。用户主动复制诊断信息或提交日志前，仍应检查其中可能出现的设备名称、文件路径、系统错误信息和服务请求标识。

## 网络访问

正常使用中可能产生以下网络访问：

- 豆包语音识别 WebSocket：连接测试、认证和听写；火山引擎 HTTPS API：创建、读取、更新或删除用户账户下的专用常用词表；
- GitHub Releases：应用启动或用户手动操作时检查新版本；用户确认后下载并安装签名更新包。

除上述用途外，VoicePaste 不主动向开发者发送使用数据。

## 联系

隐私问题或安全报告请通过 [GitHub Issues](https://github.com/imbytecat/voicepaste/issues) 联系维护者。提交公开 Issue 时不要粘贴 API Key、录音、完整识别内容或其他敏感信息。
